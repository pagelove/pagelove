# Developing in {{ project_name }}

{{ project_name }} is a platform for building web applications using standard HTML and CSS, with a unique server-side component (DOMFs) that persists state directly in the DOM.

This document serves as the reference for LLMs and developers building applications on {{ project_name }}.
